name: Uptime

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  health:
    runs-on: ubuntu-latest
    steps:
      - run: curl --fail --silent --show-error --max-time 15 https://sahabatcerdikdm.com/api/health | grep '"status":"ok"'
